apiVersion: apps/v1
kind: Deployment
metadata:
  name: igrisce
  namespace: golfapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: igrisce
  template:
    metadata:
      labels:
        app: igrisce
    spec:
      containers:
      - name: igrisce
        image: willy161/igrisce:v1
        ports:
        - containerPort: 8080
        env:
        - name: SPRING_DATASOURCE_URL
          value: "jdbc:postgresql://golfapp.postgres.database.azure.com:5432/postgres3?sslmode=require"
        - name: SPRING_DATASOURCE_USERNAME
          valueFrom:
            secretKeyRef:
              name: db-credentials-postgres2
              key: username
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db-credentials-postgres2
              key: password
        - name: SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE
          value: "5"
        - name: SPRING_DATASOURCE_HIKARI_MINIMUM_IDLE
          value: "2"
        - name: TURNIRJI_SERVICE_URL
          value: "http://turnirji:8080"
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          tcpSocket:
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 10
        readinessProbe:
          tcpSocket:
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: igrisce
  namespace: golfapp
spec:
  selector:
    app: igrisce
  ports:
  - port: 8080
    targetPort: 8080
  type: ClusterIP
